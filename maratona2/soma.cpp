#include <bits/stdc++.h>

using namespace std;

int main(){
    int x, y, soma;

    cout << "Digite o valor de x: ";
    cin >> x;
    cout << "Digite o valor de y: ";
    cin >> y;

    soma = x + y;

    cout << "Soma: " << soma;

}